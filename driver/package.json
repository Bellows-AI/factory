{
    "name": "@factory-ai/driver",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc -b && mkdir -p dist/scripts && cp -R src/scripts/. dist/scripts/",
        "dev": "tsx watch --env-file-if-exists=../.env src/index.ts"
    },
    "devDependencies": {
        "tsx": "^4.19.0"
    }
}
