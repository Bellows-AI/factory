environment:
    image: node:24
    gates:
         - name: test
           command: "npm test"
         - name: lint
           command: "npm run lint"

services:
    - name: timescale
      image: timescale/timescaledb:latest-pg17
      environment:
          POSTGRES_USER: factory
          POSTGRES_PASSWORD: factory
          POSTGRES_DB: factory_test
